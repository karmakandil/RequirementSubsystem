// recruitment-backend/src/offboarding/offboarding.service.ts

import { Injectable } from '@nestjs/common';

@Injectable()
export class OffboardingService {
  // Intentionally left empty for now.
  // We are focusing only on schemas for this milestone.
}
